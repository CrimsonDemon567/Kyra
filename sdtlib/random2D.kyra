def rand2(seed):
    x = (seed * 123.456) % 1
    y = (seed * 789.123) % 1
    return x, y

def rand_unit(seed):
    x, y = rand2(seed)
    l = sqrt(x*x + y*y)
    if l == 0:
        return 1, 0
    return x/l, y/l
